[package]
name = "pcs"
version.workspace = true
edition.workspace = true
publish.workspace = true

# `std`, like `curve` and `srs`: this crate pairs, and no guest does.
[dependencies]
constants.workspace = true
field.workspace = true
curve.workspace = true
poly.workspace = true
srs.workspace = true
transcript.workspace = true
rayon.workspace = true

[dev-dependencies]
test-support.workspace = true
# Acceptance 4's cross-check: a reduced sumcheck claim opened at its own point.
sumcheck.workspace = true
# Also a real dependency; repeated here so the integration tests can use it to
# build the toy SRS, which a test binary cannot reach through the library.
rayon.workspace = true
